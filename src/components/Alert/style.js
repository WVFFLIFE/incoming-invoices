import {makeStyles} from '@material-ui/core';

export const useStyles = makeStyles({
  titleRoot: {
    fontSize: 14,
    fontFamily: 'Proxima Nova',
    fontWeight: 500,
    color: '#30344B'
  },
  message: {
    padding: '11px 0'
  }
})